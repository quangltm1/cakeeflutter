import 'package:flutter/material.dart';

class TrangChuUserPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Trang Chủ'),
      ),
      body: Center(
        child: Text('Welcome to Trang Chủ!'),
      ),
    );
  }
}