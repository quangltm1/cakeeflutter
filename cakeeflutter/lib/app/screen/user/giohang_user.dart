import 'package:flutter/material.dart';

class GioHangPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Giỏ Hàng'),
      ),
      body: Center(
        child: Text('Đây là trang giỏ hàng của bạn'),
      ),
    );
  }
}